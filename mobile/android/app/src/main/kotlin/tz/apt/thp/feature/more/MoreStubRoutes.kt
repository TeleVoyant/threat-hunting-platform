package tz.apt.thp.feature.more

/**
 * Placeholder file. All More-tab routes now ship with real implementations
 * in their own files:
 *
 *   - SettingsRoute        → SettingsRoute.kt
 *   - PendingRoute         → PendingRoute.kt
 *   - AboutRoute           → AboutRoute.kt
 *   - EnrollmentRoute      → EnrollmentRoute.kt
 *   - DiagnosticsRoute     → DiagnosticsRoute.kt
 *   - AllowlistRoute       → AllowlistRoute.kt
 *   - HardeningRoute       → HardeningRoute.kt
 *   - SecretsRoute         → SecretsRoute.kt
 *
 * Kept as a marker so historic imports from `tz.apt.thp.feature.more` keep
 * resolving without a directory-empty error.
 */
